[project]
name = "ai-workflow-foundation"
version = "0.1.0"
description = "Local-first personal AI workflow foundation."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

